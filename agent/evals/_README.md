Compaction quality evals live in the isolated ../../evals/compaction target.
Run `npm run eval:compaction` from the repository root. It stages the production
summarizer with native Harnest eval sets, custom metrics and rubric judging,
without loading Dextana's desktop tools or persistent user data.

This folder remains available for future evaluations of the full desktop agent.
